<%= simple_form_for(@fitment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :part %>
    <%= f.association :vehicle %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
