<%= simple_form_for(@vehicle_model) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :brand %>
    <%= f.association :vehicle_type %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
